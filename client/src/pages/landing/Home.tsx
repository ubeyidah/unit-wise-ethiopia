import Header from "@/components/landing/Header";
import Hero from "@/components/landing/Hero";

const Home = () => {
  return (
    <main>
      <Header />
      <Hero />
    </main>
  );
};

export default Home;
